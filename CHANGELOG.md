# 更新日志 (Changelog)

本项目所有值得记录的变更都会记录在此文件中。

格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，
版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

变更类型说明：

- `新增`：新功能
- `变更`：对现有功能的调整
- `弃用`：即将移除的功能
- `移除`：已移除的功能
- `修复`：缺陷修复
- `安全`：安全相关修复

---

## [1.2] - 2026-09-13

### 新增

- **Android TV 支持**：
  - 引入专为大屏优化的 `TvMainScreen` 和 `TvSettingsScreen`。
  - 新增 `TvAppItem` 组件，支持遥控器（D-Pad）导航、焦点缩放及高对比度选中反馈。
  - 在 TV 端集成筛选功能、扫描进度条及操作菜单对话框。
  - 适配 TV 端本地化逻辑，确保跨语言环境下的资源同步。
- **全局架构增强**：引入全局 `APP` 类，统一处理 Shizuku 初始化、设备模式检测（`isTvMode`）及全局调试状态（
  `isNoisyMode`）。

### 变更

- **Shizuku 交互优化**：将 `ShizukuStatusCard` 从主界面迁移至设置界面，简化主流程并减少视觉干扰。
- **配置管理优化**：
  - 将默认配置导入 URL 指向 GitHub Master 分支，确保持续获取最新通用配置。
  - 导出配置文件时不再携带版本号后缀，规范化文件管理。
- **TV 视觉优化**：
  - 为 TV 端引入自定义高辨识度焦点配色（如琥珀金或高对比度黑白反转）。
  - 在 TV 主界面补全 `LinearProgressIndicator` 扫描进度条。

### 修复

- **国际化修复**：彻底解决在手动切换应用语言后，部分 Toast 提示、筛选器标签及列表状态无法立即同步翻译的
  BUG。
- **稳定性增强**：
  - 优化 `ShizukuUserService` 日志处理，接入「吵闹模式」控制，减少 ADB/Root 进程的无效日志输出。
  - 修正了 TV 端部分交互组件点击无效或焦点丢失的问题。

## [1.1] - 2026-09-12

### 新增

- **操作增强**：应用操作菜单新增「复制包名」功能。
- **筛选功能**：筛选菜单新增「只显示有配置的」项，支持仅展示已配置更新 URL 的应用。
- **导入优化**：URL 导入支持默认填充对应版本的官方 Release 配置文件地址，并可持久化记录用户最后的输入。
- **搜索优化**：优化搜索模式下的 UI 布局，确保功能按钮不被遮挡，并新增搜索无结果时的“一键重置筛选”功能。
- **更新日志查看**：支持在设置页点击「构建时间」直接弹出并查看当前版本的详细更新内容。

### 变更

- **开发体验**：为 `debug` 构建变体添加 `.debug` 包名后缀，支持在同一设备上与正式版共存。
- **筛选逻辑优化**：
  - 将安装来源为自身包名的应用统一归类为「自更新软件」。
  - 优化筛选条目排序规则：系统应用 > ADB 安装 > 自更新应用 > 普通市场。
  - 优化展示标签提取逻辑，优先使用已安装市场的友好名称。
- **发布流程**：GitHub Release 页面正文改为「构建元信息 + 自动生成的变更清单」结构。
- **Android 14 兼容性**：除 Binder 稳定性增强外，进一步完善了系统底层权限的适配。

### 修复

- **稳定性增强**：修复在应用扫描过程中重复触发刷新可能导致的崩溃问题。
- **筛选修复**：修复由于本地化标签匹配失效导致某些翻译过的应用（如 Google Play）无法被筛选的问题。
- **配置变更响应**：引入配置变更响应机制，支持应用感知并响应配置文件的动态变化。

---

## [1.0] - 2026-09-12

首个正式版本。

### 新增

- **安装来源识别**：基于 Shizuku 还原应用的真实安装来源，自动区分 `Originating`（原始来源）与
  `Initiating`（发起者），并支持多用户安装检测。
- **搜索与筛选**：支持「应用名 + 包名」多关键词混合搜索，并可按系统应用、已禁用状态及安装来源筛选。
- **自定义更新管理**：为任意应用配置更新检查 URL，支持一键跳转应用商店或自定义更新地址。
- **配置导入与导出**：
    - 支持通过本地 JSON 文件、远程 URL 链接导入配置。
    - 支持导出并查看格式化后的 JSON 配置文件。
    - 支持同一局域网内设备间的配置快速共享（NSD 发现 + 内置 TCP 服务端）。
- **设置与外观**：支持深色模式、系统外观跟随与多语言（简体中文 / English）。
- **应用列表**：流式增量加载与实时进度追踪，应用列表项支持更多操作菜单。
- **构建与发布**：集成 GitHub Actions 自动构建流水线，推送至 `release` 分支即可自动产出已签名 APK
  与对应版本配置。

### 变更

- 重构安装信息获取逻辑，改用 Shizuku User Service 实现。

### 修复

- 针对 Android 14+ 进行 Binder 稳定性增强，支持 Shizuku 服务断开自动重连与异常防护。
- 修复 `AndroidManifest.xml` 中的 Lint 静态检查警告。

[未发布]: https://github.com/houfukude/UpdateJunkie/compare/v1.2...HEAD

[1.2]: https://github.com/houfukude/UpdateJunkie/releases/tag/v1.2

[1.1]: https://github.com/houfukude/UpdateJunkie/releases/tag/v1.1

[1.0]: https://github.com/houfukude/UpdateJunkie/releases/tag/v1.0
